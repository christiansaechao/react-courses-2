import React, { useState } from "react";

/**
 * 1. Build a form using a single useState
 * 2. Display the form in a preview next to the form as you're filling it out
 * 3. use useState to keep track of all form information
 */

const Form = () => {

  return (
    <div>
    </div>
  );
};

export default Form;
